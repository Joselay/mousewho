{
  "id": "aee971f6",
  "title": "Add Firefox build for Mousewho",
  "tags": [
    "firefox",
    "extension"
  ],
  "status": "closed",
  "created_at": "2026-06-11T04:51:43.173Z"
}

Completed Firefox build support.

Implemented:
- Shared `src/` now uses `globalThis.browser || globalThis.chrome` for extension APIs.
- Added `manifest.firefox.json` with Firefox background scripts and Gecko settings.
- Added `scripts/build-firefox.js` to copy `src/`, `icons/`, and Firefox manifest into `dist/firefox/`.
- Added `npm run build:firefox`.
- Added `.gitignore` for generated `dist/` output.
- Updated manifest tests for Chrome and Firefox manifests.
- Updated README with Firefox loading instructions.

Verified:
- `npm test` passes.
- `npm run build:firefox` creates `dist/firefox/manifest.json`, `src/`, and `icons/`.

Manual QA still needed in Firefox via `about:debugging#/runtime/this-firefox`.
