{
  "name": "mousewho",
  "version": "0.1.0",
  "private": true,
  "description": "Blazing-fast keyboard-only browser control for Vim-style browsing.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js && node --check scripts/build-firefox.js && node --check src/background.js && node --check src/content.js && node --check src/dom.js && node --check src/hints.js",
    "build:firefox": "node scripts/build-firefox.js",
    "benchmark": "node benchmark/hints-benchmark.js"
  },
  "keywords": [
    "chrome-extension",
    "firefox-extension",
    "vim",
    "keyboard",
    "browser"
  ],
  "license": "MIT"
}
